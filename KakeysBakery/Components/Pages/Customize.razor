@page "/Customize"
@inject IAddonService addon
@inject IBaseGoodService basegood
@inject IProductService product
@inject ICustomerService customer

<body>
<div class="mt-5 bg-light border vh-100 ">
    <div class="d-flex flex-wrap justify-content-between p-5">
        @for (int i = 0; i < 5; i++)
        {
            <div class="p-3 d-flex flex-column ">
                <img src="images/cookie.jpg" class="imgbutton img-fluid img-thumbnail rounded-5 shadow">
                <p class="text-center">look at my description</p>
            </div>
        }
    </div>

    <div class="m-4">
        <div class="d-flex flex-row">
            <label class="me-3">Icing Flavor</label>
            <select class="form-select-sm" />
        </div>

        <div class="d-flex flex-row">
            <label class="me-4">Icing Type</label>
            <select class="form-select-sm" />
        </div>

    </div>

    <div class="m-4">
        <label>Layers</label>
        <input type="number" />
        <label>Count</label>
        <input type="number" />
    </div>

    <div class="m-4">

        <h1>Total Price: </h1>
        <p>Tax not included, will be added upon checkout</p>
    </div>
    <button class="m-4">Add To Cart</button>
</div>
</body>

@code {

}
