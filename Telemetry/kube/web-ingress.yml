apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  namespace: ${NAMESPACE}
  name: kakey-blazor-ingress
spec:
  ingressClassName: nginx
  rules:
  - host: $PULL_REQUEST_ID.kakey-telemetry-ts.duckdns.org
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: kakey-blazor-service
            port:
              number: 8080