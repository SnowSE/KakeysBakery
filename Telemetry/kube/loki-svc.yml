apiVersion: v1
kind: Service
metadata:
  namespace: kakey-1
  name: loki-service

apiVersion: v1
kind: Service
metadata:
  namespace: kakey-1
  name: loki-service
spec:
  type: ClusterIP
  selector:
    app: kakey-loki
  ports:
    - port: 3100
      targetPort: 3100